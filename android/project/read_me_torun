
phele android sdk install kar liyo pc me
or developer mode on karke
android debugging on kar liyo 

pip install --user --upgrade websockets(install the servers)

uvicorn admin_test_server:app --host 0.0.0.0 --port 9000 --ws none(run this in terminal to run the server with the same file)

flutter run --dart-define=FLASHGUARD_API_BASE_URL=https://flashguard-ai.onrender.com --dart-define=FLASHGUARD_ADMIN_BASE_URL=http://192.168.1.103:9000
(run this in the app folder to run the app using the server)
