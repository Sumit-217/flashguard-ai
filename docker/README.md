# Docker Configurations

This directory contains Dockerfiles and container configurations for FlashGuard AI services (PostGIS, Mosquitto MQTT, FastAPI backend, React dashboard, OSRM).

> **Note**: Specific Docker configurations will be created during implementation stages.
